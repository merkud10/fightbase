name: "Sync Events + Odds"

on:
  schedule:
    # Каждый день в 06:00 UTC (09:00 МСК)
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual trigger'
        required: false
        default: 'Manual sync'

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Trigger odds sync via cron endpoint
        id: sync
        run: |
          response=$(curl -sf -w "\n%{http_code}" -X POST "${{ secrets.NEXT_PUBLIC_SITE_URL }}/api/cron/ingest" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.INGEST_CRON_SECRET }}" \
            -d '{"job":"sync-odds"}')

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n -1)

          echo "HTTP status: $http_code"
          echo "Response: $body"

          if [ "$http_code" != "200" ]; then
            echo "sync_ok=false" >> "$GITHUB_OUTPUT"
            echo "Odds sync failed with HTTP $http_code"
            exit 1
          fi

          echo "sync_ok=true" >> "$GITHUB_OUTPUT"

      - name: Wait for background job to process
        run: |
          sleep 30
          for i in 1 2 3 4 5 6; do
            curl -sf -X POST "${{ secrets.NEXT_PUBLIC_SITE_URL }}/api/cron/jobs" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.INGEST_CRON_SECRET }}" \
              -d '{"limit": 3}' || true
            sleep 20
          done

      - name: Send Telegram notification
        if: steps.sync.outputs.sync_ok == 'true'
        run: |
          curl -sf -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{\"chat_id\": \"${{ secrets.TELEGRAM_ALERTS_CHAT_ID }}\", \"text\": \"✅ Турниры + бои + прогнозы обновлены\", \"disable_notification\": true}"
